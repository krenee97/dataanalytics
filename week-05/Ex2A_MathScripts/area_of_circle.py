# calculate the area of a circle

# Define the diameter ( using birth day)
diameter = 14 
radius = diameter / 2

# Calcualte the area (pi * radius squared)
pi = 3.14159
area = pi * radius * radius

# Display results 
print("The area of a circle with radius " + str(radius) + " is " + format(area, ".2f"))
